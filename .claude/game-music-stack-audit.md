# Game Music Production Stack — Install Audit

Project: `lifepatch` (Next.js + TypeScript browser game). Host: macOS arm64 (Darwin 25.5). Generated by Claude Code during stack install.

Statuses: ✅ installed & verified · ⚠️ installed but needs auth/API/login · 🟡 optional/pending (missing dependency / manual step) · 🚫 quarantined/security review · ❌ failed with exact error.

| # | Component | Type | Install target | Verification evidence | Status |
|---:|---|---|---|---|---|
| 1 | game-audio-director skill | Project skill (markdown) | `.claude/skills/game-audio-director/SKILL.md` | File written; appears in Skill registry (`game-audio-director`) | ✅ |
| 2 | beat-sync-composer skill | Project skill | `.claude/skills/beat-sync-composer/SKILL.md` | File written; registered (`beat-sync-composer`) | ✅ |
| 3 | adaptive-music-engine skill | Project skill | `.claude/skills/adaptive-music-engine/SKILL.md` | File written; registered (`adaptive-music-engine`) | ✅ |
| 4 | cinematic-audio-qc skill | Project skill | `.claude/skills/cinematic-audio-qc/SKILL.md` | File written; registered (`cinematic-audio-qc`) | ✅ |
| 5 | music-composition skill | Cloned skill | `.claude/skills/music-composition/` | `git clone` ok; `SKILL.md` present; frontmatter = theory/composition; registered. Nested `.git` removed. | ✅ |
| 6 | code-to-music skill | Downloaded skill | `.claude/skills/code-to-music/` | curl HTTP 200, valid zip (28 KB) → `SKILL.md`; frontmatter states "Never use … for replicating songs, beats, riffs" (anti-copy); algorithmic via music21; registered. | ✅ |
| 7 | ai-music-generation skill | Installed skill | `.claude/skills/ai-music-generation/SKILL.md` | `npx skills add` copied it; registered. Requires inference.sh `belt` CLI + `belt login` (**paid**). `belt` MISSING. **Security:** installer showed Socket "1 alert" (Snyk Low / Gen Safe); SKILL.md reviewed = plain docs, `allowed-tools: Bash(belt *)` only, no `curl\|sh`/eval/exfiltration. | ⚠️ |
| 8 | Bitwize music plugin | Claude plugin | marketplace `bitwize-music`; plugin `bitwize-music@bitwize-music` | `claude plugin install` → "Successfully installed". Bundles MCP `bitwize-music-mcp` needing `~/.bitwize-music/venv` (absent → ✗ failed to connect). Setup not run by assistant. | ✅ plugin / 🟡 MCP+setup |
| 9 | Replicate MCP | Remote MCP (SSE) | user config `~/.claude.json` | `claude mcp add` ok; `claude mcp list` → "replicate … ! Needs authentication". Code mode skipped (Deno + `REPLICATE_API_TOKEN` MISSING). | ⚠️ |
| 10 | ElevenLabs MCP | Local MCP (uvx) | not added | `ELEVENLABS_API_KEY` MISSING **and** `uvx`/`uv` MISSING. Not installed (would fail). | ⚠️ |
| 11 | Vibe Composer MIDI MCP | Local MCP (npx) | user config `~/.claude.json` | `claude mcp add` ok; `claude mcp list` → "vibe-composer-midi … ✓ Connected". Limitation: no hardware MIDI output on this host. | ✅ |
| 12 | Ableton producer skills | Cloned skill | not installed | Ableton Live NOT in `/Applications`. Not cloned (per rules). | 🟡 |
| 13 | Ableton MCP | Local MCP | not installed | Ableton Live absent; MCP not added; no Remote Scripts touched. | 🟡 |
| 14 | Tone.js | npm dep | `package.json` | `npm install` ok; `tone@^15.1.22` present; game `npm run build` ✓. | ✅ |
| 15 | @tonejs/midi | npm dep | `package.json` | `@tonejs/midi@^2.0.28` present. | ✅ |
| 16 | howler.js | npm dep | `package.json` | `howler@^2.2.4` (+`@types/howler@^2.2.13`) present. | ✅ |
| 17 | Python audio venv | venv + pip | `.venv-audio/` (Python 3.14.3) | All 9 import: `librosa 0.11.0`, soundfile, `numpy 2.4.6`, scipy, pretty_midi, mido, `music21 10.5.0`, pydub, pyloudnorm → "audio python imports ok". Fix: Py3.14 removed `audioop`, added `audioop-lts` for pydub. `analyze-audio.py` tracked beats on a test WAV. | ✅ |
| 18 | ffmpeg | system binary | not installed | `command -v ffmpeg` = MISSING. Needs your approval: `brew install ffmpeg`. (pydub/librosa mp3 + normalization need it.) | 🟡 |
| 19 | RunComfy CLI | npm global | global node | `npm i -g @runcomfy/cli` ok → `runcomfy 0.1.1`. Use requires login/token (no credits spent). | ⚠️ |

## Pending actions (yours)
- **Replicate**: run `/mcp` in Claude Code → authenticate `replicate`. (Optional code mode also needs Deno + `REPLICATE_API_TOKEN`.)
- **ElevenLabs MCP**: set `ELEVENLABS_API_KEY` as an env var (don't paste in chat) and install `uv` (`brew install uv`), then I can add it. (Note: a separate ElevenLabs/Higgsfield `generate_audio` MCP is already connected in this Claude session as an alternative.)
- **ai-music-generation**: install the `belt` CLI + `belt login` (inference.sh, paid). Approve first.
- **Bitwize**: run `/bitwize-music:setup` manually (creates `~/.bitwize-music/venv`; may want a Suno account/credits — approve first).
- **ffmpeg**: approve `brew install ffmpeg`.
- **RunComfy**: `runcomfy login` (approve first; credits cost).
- **Ableton**: install Ableton Live, then I can add Ableton skills + MCP.

## Failed / notable command outputs
- `claude plugin install bitwize-music@claude-ai-music-skills` → `Plugin "bitwize-music" not found in marketplace "claude-ai-music-skills"` (wrong id; succeeded as `bitwize-music@bitwize-music`).
- `pip install pydub` import → `ModuleNotFoundError: No module named 'pyaudioop'` (Python 3.14 PEP 594) → fixed with `audioop-lts`.
- `timeout` not available on macOS (used Bash tool timeout instead).
- pydub runtime warning: "Couldn't find ffmpeg or avconv" (expected until ffmpeg installed).

## Originality / safety posture
No copyrighted audio, samples, or "in the style of [artist]" prompts. References = high-level design language only. MIDI-first composition + beat maps before any AI render. Every cue requires `*.music.json` metadata + cinematic-audio-qc pass before "finished".
